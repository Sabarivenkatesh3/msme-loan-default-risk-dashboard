-- Overall portfolio health metrics
SELECT
    COUNT(*) AS total_borrowers,
    SUM(loan_amount) AS total_portfolio_inr,
    ROUND(AVG(loan_amount), 0)  AS avg_loan_size,
    SUM(CASE WHEN loan_status = 'NPA'       THEN 1 ELSE 0 END) AS npa_count,
    ROUND(100.0 * SUM(CASE WHEN loan_status = 'NPA' THEN 1 ELSE 0 END)
          / COUNT(*), 2) AS npa_rate_pct,
    ROUND(100.0 * SUM(CASE WHEN dpd_current > 30 THEN 1 ELSE 0 END)
          / COUNT(*), 2) AS par_30_pct,
    ROUND(100.0 * SUM(CASE WHEN dpd_current > 60 THEN 1 ELSE 0 END)
          / COUNT(*), 2) AS par_60_pct
FROM msme_loans_clean;